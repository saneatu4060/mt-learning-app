import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // images: {
  //   domains: ['lh3.googleusercontent.com'], // ここにホスト名を追加
  // },
  images: {
    domains: ['localhost'], // 画像を置いているドメイン
  },
};


export default nextConfig;
